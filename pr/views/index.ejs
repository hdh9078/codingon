<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <title>Document</title>
    <script
      src="https://kit.fontawesome.com/837ff3adc1.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <h1>회원가입</h1>
    <form name="register">
        <input type="text" placeholder="이름" id="name">
        <br>
       <fieldset>
        <legend>성별</legend>
        <input type="radio" name="gender" value="남자">남자
        <input type="radio" name="gender" value="여자">여자
       </fieldset>
       <br>
       <fieldset>
        <legend>생년월일</legend>
        <select name="year" id="year">
            <% for(let i = 1990; i <=2010; i++) {%>
                <option value="<%= i %>"><%= i %></option>
            <% } %>
        </select>
        <select name="month" id="month">
            <% for(let i = 1; i <= 12; i++) {%>
                <option value="<%= i %>"><%= i %></option>
            <%}%>
        </select>
        <select name="day" id="day">
            <% for(let i = 1; i <= 31; i++) {%>
                <option value="<%= i %>"><%= i %></option>
            <%}%>
        </select>
       </fieldset>
       <br>
       <fieldset>
        <legend>관심사</legend>
        <input type="checkbox" name="ho1" id="ho1" value="여행">여행
        <input type="checkbox" name="ho1" id="ho1" value="패션">패션
        <input type="checkbox" name="ho1" id="ho1" value="음식">음식
       </fieldset>
       <button type="button" onclick="axiosGet()">회원가입</button>
    </form>
    <div class="result"></div>
    <a href="/log">post</a>

  <script>
      const resultBox = document.querySelector(".result");
      function axiosGet() {
        const form = document.forms["register"];
        const query = 'input[name="ho1"]:checked';
        const hobby = document.querySelectorAll(query);
        let result = " ";
        hobby.forEach((el) => {
          result += el.value + "";
        })
        const data = {
          name: form.name.value,
          gender: form.gender.value,
          year: form.year.value,
          month: form.month.value,
          day: form.day.value,
          hobby: result,
        }
        axios ({
          method:"get",
          url:"/axios",
          params: data,
        }).then((res) => {
          const {name, gender, year, month, day, hobby} = res.data;
          resultBox.textContent = `회원가입완료 ${name}님은 ${gender}이시고, 생일은${year}년 ${month}월 ${day}일 입니다. 관심사는 ${hobby} 입니다.`
        })
      }
  </script>
  </body>
</html>